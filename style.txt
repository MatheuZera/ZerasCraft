/*
 * style.css - Estilos para o site Mundo Zera's Craft
 * Gerado com base na estrutura HTML fornecida.
 */

/* ===================================== */
/* 0. Variáveis e Reset Básico           */
/* ===================================== */

:root {
    /* Cores */
    --cor-primaria: #4CAF50; /* Verde vibrante para botões e destaques */
    --cor-secundaria: #388E3C; /* Um verde um pouco mais escuro, para hover, por exemplo */
    --cor-terciaria: #2E7D32; /* Verde mais escuro para fundos alternativos */
    --cor-texto-claro: #ECF0F1; /* Cinza claro para textos em fundos escuros */
    --cor-texto-escuro: #2C3E50; /* Azul escuro para textos em fundos claros */
    --cor-fundo-claro: #F8F9FA; /* Branco quase puro para seções claras */
    --cor-fundo-escuro: #1A1A1A; /* Preto quase puro para fundos escuros */
    --cor-destaque: #FFD700; /* Dourado para ícones de destaque ou títulos */
    --cor-bordas: rgba(255, 255, 255, 0.1); /* Bordas sutis */
    --cor-card-bg: #2C3E50; /* Fundo dos cards (azul escuro) */
    --cor-card-text: #ECF0F1; /* Texto dos cards (cinza claro) */
    --cor-card-hover: #34495E; /* Fundo dos cards ao passar o mouse */
    --cor-link: #8bc34a; /* Cor de link padrão */
    --cor-link-hover: #aed581; /* Cor de link ao passar o mouse */
    --cor-btn-text: #FFFFFF; /* Texto dos botões */
    /* Cores do neon (ajuste para o seu verde/ciano neon) */
    --neon-flame-color1: #00FF00; /* Verde Neon */
    --neon-flame-color2: #00FFFF; /* Ciano Neon */
    --neon-flame-color1-rgb: 0, 255, 0; /* RGB para sombras */
    --neon-flame-color2-rgb: 0, 255, 255; /* RGB para sombras */

    --border-radius: 8px;
    --fonte-titulo: 'Arial', sans-serif; /* Exemplo, use sua fonte real */
    --cor-fundo-card: #2c2f33; /* Cor de fundo dos cards */
    --cor-texto-card: #e0e0e0; /* Cor do texto dos cards */
    --cor-borda-card-hover: #4CAF50; /* Cor da borda do card no hover */


    /* Fontes */
    --fonte-titulo: 'Montserrast', sans-serif;
    --fonte-corpo: 'Roboto', sans-serif;

    /* Espaçamentos e Tamanhos */
    --padding-section: 80px 0; /* Preenchimento padrão para seções */
    --padding-card: 25px; /* Preenchimento interno dos cards */
    --gap-grid: 30px; /* Espaço entre itens de grid */
    --border-radius: 8px; /* Borda arredondada padrão */
    --max-width-content: 1200px; /* Largura máxima do conteúdo */

    /* Sombras */
    --sombra-leve: 0 4px 8px rgba(0, 0, 0, 0.2);
    --sombra-card: 0 8px 16px rgba(0, 0, 0, 0.4);

    /* Transições */
    --transicao-padrao: all 0.2s ease;
    --transicao-card-hover: transform 0.3s ease-out, box-shadow 0.3s ease-out, background-color 0.3s ease; /* Transição específica para hover de card */
}
/* Estilos gerais para o corpo da página (apenas para contexto) */
body {
    font-family: Arial, sans-serif;
    background-color: #1a1a1a; /* Fundo escuro */
    color: #f0f0f0;
    margin: 0;
    padding: 0;
    overflow-x: hidden; /* Evita barra de rolagem horizontal */
}

/* Reset Básico para consistência entre navegadores */
*, *::before, *::after {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: var(--fonte-corpo);
    line-height: 1.6;
    color: var(--cor-texto-claro);
    background-color: var(--cor-fundo-escuro);
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
}

/* Base para links */
a {
    color: var(--cor-link);
    text-decoration: none;
    transition: var(--transicao-padrao);
}

a:hover {
    color: var(--cor-link-hover);
    text-decoration: underline;
}

/* Base para títulos */
h1, h2, h3, h4, h5, h6 {
    font-family: var(--fonte-titulo);
    color: var(--cor-texto-claro);
    margin-bottom: 0.8em;
    line-height: 1.2;
}

h1 { font-size: 3.5em; }
h2 { font-size: 2.8em; }
h3 { font-size: 2.2em; }
h4 { font-size: 1.8em; }

p {
    margin-bottom: 1em;
}

ul {
    list-style: none;
}

/* Classes de utilidade para espaçamento */
.margin-bottom-30 { margin-bottom: 30px !important; }
.margin-bottom-40 { margin-bottom: 40px !important; }
.text-center { text-align: center; }

/* ===================================== */
/* 1. Layout Geral e Contêineres         */
/* ===================================== */

.container {
    max-width: var(--max-width-content);
    margin: 0 auto;
    padding: 0 20px;
}

.section-padded {
    padding: var(--padding-section); /* Onde --padding-section é 80px 0; */
}

.section-alt-bg {
    background-color: var(--cor-terciaria); /* Fundo verde mais escuro */
}

/* Flexbox para duas colunas */
.content-two-columns {
    display: flex;
    flex-wrap: wrap; /* Permite quebrar linha em telas menores */
    gap: var(--gap-grid);
    align-items: center;
}

.content-two-columns .text-content,
.content-two-columns .image-content {
    flex: 1; /* Permite que ambos os itens cresçam igualmente */
    min-width: 300px; /* Garante que os itens não fiquem muito estreitos */
}

.responsive-image {
    max-width: 100%;
    height: auto;
    display: block; /* Remove espaçamento extra abaixo da imagem */
    border-radius: var(--border-radius);
}

.bordered-image {
    border: 3px solid var(--cor-primaria); /* Borda primária para imagens de destaque */
    box-shadow: var(--sombra-card);
}

/* ===================================== */
/* 2. Cabeçalho (Header)                 */
/* ===================================== */

.main-header {
    background-color: var(--cor-fundo-escuro);
    padding: 20px 0;
    box-shadow: var(--sombra-leve);
    position: sticky;
    top: 0;
    z-index: 1000;
    border-bottom: 1px solid var(--cor-bordas);
}

.header-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap; /* Para navegação em telas menores */
}

.header-logo {
    font-family: var(--fonte-titulo);
    font-size: 1.8em;
    color: var(--cor-primaria);
    text-transform: uppercase;
    font-weight: 700;
    letter-spacing: 1px;
}

.main-nav ul {
    display: flex;
    gap: 25px;
}

.main-nav ul li a {
    color: var(--cor-texto-claro);
    font-weight: 700;
    padding: 5px 0;
    position: relative;
}

.main-nav ul li a::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 2px;
    background-color: var(--cor-destaque);
    transition: width 0.3s ease;
}

.main-nav ul li a:hover::after {
    width: 100%;
}

/* NOVO: Botão de Hambúrguer para Mobile */
.menu-toggle {
    display: none; /* Escondido por padrão em desktop */
    font-size: 2em;
    color: var(--cor-texto-claro);
    background: none;
    border: none;
    cursor: pointer;
    padding: 10px;
    z-index: 1001; /* Acima do nav quando aberto */
}

/* ===================================== */
/* 3. Seção Hero (Destaque Principal)    */
/* ===================================== */

.hero-section {
    position: relative;
    height: 650px; /* Altura fixa para o hero */
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    color: var(--cor-texto-claro);
    overflow: hidden; /* Garante que a imagem de fundo não vaze */
    background: url('https://matheuzera.github.io/ZerasCraft/images/site/banner-fundo.gif') no-repeat center center/cover; /* Imagem de fundo do hero */
}

.hero-image-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.7); /* Escurece a imagem de fundo */
    z-index: 1;
}

.hero-content {
    position: relative;
    z-index: 2;
    padding: 20px;
    max-width: 900px;
}

.server-branding {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 20px;
    margin-bottom: 20px;
}

.server-banner {
    max-width: 250px;
    border-radius: var(--border-radius);
    box-shadow: var(--sombra-leve);
}

.server-icon {
    width: 100px;
    height: 100px;
    border-radius: 50%; /* Torna o ícone circular */
    border: 4px solid var(--cor-primaria);
    box-shadow: var(--sombra-card);
}

.hero-content h1 {
    font-size: 3.8em;
    margin-bottom: 0.5em;
    text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.5);
}

.hero-subtitle {
    font-size: 1.3em;
    max-width: 700px;
    margin: 0 auto 30px auto;
    color: rgba(255, 255, 255, 0.9);
}

/* ===================================== */
/* 4. Botões e Elementos Interativos     */
/* ===================================== */

.btn-primary {
    display: inline-block;
    background-color: var(--cor-primaria);
    color: var(--cor-btn-text);
    padding: 15px 30px;
    border-radius: var(--border-radius);
    text-transform: uppercase;
    font-weight: 700;
    letter-spacing: 1px;
    transition: var(--transicao-padrao);
    border: none;
    cursor: pointer;
    font-size: 1.1em;
}

.btn-primary:hover {
    background-color: var(--cor-secundaria);
    transform: translateY(-3px);
    box-shadow: var(--sombra-leve);
    text-decoration: none;
}

.btn-link {
    display: inline-block;
    color: var(--cor-primaria);
    font-weight: 700;
    text-decoration: underline;
    margin-top: 15px;
    transition: var(--transicao-padrao);
}

.btn-link:hover {
    color: var(--cor-destaque);
    text-decoration: none;
    transform: translateX(5px);
}

.discord-channel-link {
    color: var(--cor-link);
    font-weight: bold;
}

.discord-channel-link:hover {
    color: var(--cor-link-hover);
    text-decoration: underline;
}

/* Estilo para ícones em títulos */
h2 .fas, h3 .fas {
    color: var(--cor-destaque);
    margin-right: 10px;
}

/* ===================================== */
/* 5. Seções com Grids                   */
/* ===================================== */

.section-description {
    text-align: center;
    font-size: 1.1em;
    max-width: 800px;
    margin: 0 auto 40px auto;
    color: rgba(255, 255, 255, 0.8);
}

/* Estilo base para todos os grids */
.services-grid,
.roles-grid,
.access-cards-grid,
.community-grid,
.events-sub-grid,
.partnership-grid { /* Adicionado partnership-grid */
    display: grid;
    gap: var(--gap-grid);
    margin-top: 50px;
}

.max-2-columns {
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
}

.max-3-columns {
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
}

.max-4-columns {
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
}

/* Estilo base para todos os cards - AGORA INCLUI TRANSFORMAÇÕES DE HOVER AQUI */
.service-card,
.role-category-card,
.access-card,
.community-card,
.partnership-card,
.event-card,
.security-card { /* Adicionado security-card */
    background-color: var(--cor-card-bg);
    border-radius: var(--border-radius);
    padding: var(--padding-card);
    text-align: center;
    box-shadow: var(--sombra-leve);
    transition: var(--transicao-card-hover); /* Usando a variável de transição para cards */
    border: 1px solid rgba(255, 255, 255, 0.05); /* Borda sutil */
    display: flex; /* Para flexibilizar conteúdo interno */
    flex-direction: column;
    justify-content: space-between; /* Empurra o botão para baixo */
}

/* Efeito hover para cards - APLICADO AO PRÓPRIO CARD */
.service-card:hover,
.role-category-card:hover,
.access-card:hover,
.community-card:hover,
.partnership-card:hover,
.event-card:hover,
.security-card:hover { /* Adicionado security-card:hover */
    transform: translateY(-5px) rotate(0.8deg); /* Card sobe um pouco e rotaciona 2 graus */
    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.6); /* Sombra mais intensa e um pouco maior */
    background-color: var(--cor-card-hover); /* Fundo do card muda */
}

/* Estilos para os ícones e imagens dentro dos cards (estáticos no hover do card) */
.service-card .card-icon,
.role-category-card .role-category-icon,
.community-card .member-avatar,
.partnership-card .partnership-logo,
.access-card .card-icon,
.security-card .card-icon { /* Adicionado security-card .card-icon */
    width: 80px; /* Tamanho padrão para ícones de card */
    height: 80px;
    object-fit: cover;
    border-radius: var(--border-radius);
    margin: 0 auto 20px auto; /* Centraliza e adiciona espaço abaixo */
    display: block; /* Garante que a transformação funcione corretamente */
    transition: none; /* REMOVE A TRANSIÇÃO NO HOVER DO ÍCONE, ELE FICARÁ ESTÁTICO */
}

/* Exceção para avatares e logos que são circulares */
.member-avatar,
.partnership-logo {
    border-radius: 50%; /* Avatares circulares */
    width: 100px;
    height: 100px;
}

/* Tratamento específico para as imagens de evento (estáticas no hover do card) */
.event-card .event-card-image {
    width: 100%;
    height: 200px;
    object-fit: cover;
    border-top-left-radius: var(--border-radius);
    border-top-right-radius: var(--border-radius);
    margin-bottom: 0;
    border-bottom: 2px solid var(--cor-primaria);
    display: block; /* Garante que ocupe o espaço */
    transition: none; /* REMOVE A TRANSIÇÃO NO HOVER DA IMAGEM, ELA FICARÁ ESTÁTICA */
}

.card-title {
    color: var(--cor-destaque);
    font-size: 1.5em;
    margin-bottom: 10px;
}

.card-description {
    color: rgba(255, 255, 255, 0.7);
    font-size: 0.95em;
    flex-grow: 1; /* Permite que o texto ocupe o espaço disponível */
    margin-bottom: 20px;
}

/* ===================================== */
/* 6. Seção de Cargos e Canais           */
/* ===================================== */

.role-category-card .role-list {
    text-align: left;
    margin-top: 15px;
    padding-left: 20px;
    list-style: disc; /* Adiciona marcadores de lista */
    color: rgba(255, 255, 255, 0.7);
    font-size: 0.9em;
}

.role-category-card .role-list li {
    margin-bottom: 5px;
}

/* ===================================== */
/* 7. Seção Pixel Legends                */
/* ===================================== */

.pixel-legends-embed {
    background-color: var(--cor-card-bg);
    border-radius: var(--border-radius);
    padding: var(--padding-card);
    margin-top: var(--gap-grid);
    box-shadow: var(--sombra-leve);
    border: 1px solid rgba(255, 255, 255, 0.05);
}

/* No seu CSS */

#pixel-legends {
    /* Mantém o padding interno da seção conforme definido em .section-padded ou em ajuste anterior */
    /* Exemplo: padding: 40px 0; */
    margin-bottom: 10px; /* Altere este valor para controlar a distância entre Pixel Legends e a próxima seção */
}

.pixel-legends-embed h4 {
    color: var(--cor-destaque);
    font-size: 1.6em;
    margin-bottom: 1em;
}

.pixel-legends-embed ul {
    list-style: inside;
    margin-bottom: 1em;
    color: rgba(255, 255, 255, 0.8);
}

.pixel-legends-embed li {
    margin-bottom: 8px;
}

/* ===================================== */
/* 8. Seção de Acesso                    */
/* ===================================== */

.access-card code {
    display: block;
    background-color: rgba(0, 0, 0, 0.3);
    padding: 10px;
    border-radius: var(--border-radius);
    margin: 10px auto;
    font-family: 'Consolas', 'Monaco', monospace;
    color: var(--cor-destaque);
    font-size: 1.1em;
    word-break: break-all; /* Garante que o texto quebre se for muito longo */
}

/* ===================================== */
/* 9. Seção de Eventos                   */
/* ===================================== */

.events-sub-grid {
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
}

.event-card {
    overflow: hidden; /* Esconde partes da imagem que podem vazar */
}

.event-card-inner {
    padding: var(--padding-card);
    text-align: left; /* Alinhamento de texto para o conteúdo do card */
    flex-grow: 1;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
}

.event-card-inner h3 {
    text-align: center;
    font-size: 1.6em;
    margin-bottom: 15px;
    min-height: 2.5em; /* Garante altura mínima para títulos de duas linhas */
}

.event-card-inner p {
    font-size: 0.9em;
    color: rgba(255, 255, 255, 0.7);
    margin-bottom: 10px;
}

.event-details {
    font-style: italic;
    font-size: 0.85em;
    color: rgba(255, 255, 255, 0.6);
    margin-top: 15px;
    margin-bottom: 20px;
}

/* ===================================== */
/* 10. Seção de Comunidade e Parcerias   */
/* ===================================== */

.partnership-proposal-card {
    background-color: var(--cor-terciaria);
    border-radius: var(--border-radius);
    padding: var(--padding-card);
    text-align: center;
    margin-top: var(--gap-grid);
    box-shadow: var(--sombra-card);
    border: 1px solid var(--cor-primaria);
}

.partnership-proposal-card h3 {
    color: var(--cor-destaque);
    margin-bottom: 15px;
}

.partnership-proposal-card p {
    color: rgba(255, 255, 255, 0.8);
    font-size: 1.05em;
    margin-bottom: 30px;
}

/* ===================================== */
/* 11. Rodapé (Footer)                   */
/* ===================================== */

.main-footer {
    background-color: var(--cor-fundo-escuro);
    padding: 30px 0;
    text-align: center;
    border-top: 1px solid var(--cor-bordas);
    color: rgba(255, 255, 255, 0.6);
    font-size: 0.9em;
}

.footer-logo {
    font-family: var(--fonte-titulo);
    font-size: 1.5em;
    color: var(--cor-primaria);
    font-weight: 700;
    display: block;
    margin-bottom: 10px;
}

/* ===================================== */
/* 12. Controle de Áudio - NOVO SISTEMA  */
/* ===================================== */

/* Container para posicionamento fixo do botão de áudio */
.audio-control-button-container {
    position: fixed;
    bottom: 30px; /* Distância da parte inferior da tela */
    right: 30px; /* Distância da parte direita da tela */
    z-index: 1050; /* Garante que o botão fique acima da maioria dos outros elementos */
    display: flex;
    justify-content: flex-end; /* Alinha o conteúdo à direita para posicionar o texto */
    align-items: center; /* Centraliza verticalmente o texto e o botão */
    gap: 15px; /* Espaço entre o texto e o botão */
}

/* Estilo base para o botão principal de controle de áudio */
#audioControlButton.audio-control-button {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 60px; /* Largura fixa para o círculo */
    height: 60px; /* Altura fixa para o círculo */
    border-radius: 50%; /* Transforma o botão em um círculo perfeito */
    background-color: transparent; /* Fundo transparente para mostrar apenas o neon */
    color: var(--cor-btn-text); /* Cor do ícone */
    cursor: pointer;
    border: 2px solid transparent; /* Borda transparente inicial */
    box-shadow: none; /* Sem sombra padrão */
    overflow: hidden; /* Importante para o SVG e o efeito neon */
    transition: transform 0.2s ease, box-shadow 0.3s ease; /* Adicione box-shadow para transição suave */
    padding: 0; /* Remover padding para centralizar melhor o ícone */
}

/* Efeito de hover para o botão (sempre, pois o neon já é animado quando tocando) */
#audioControlButton.audio-control-button:hover {
    transform: scale(1.05); /* Efeito sutil de "crescer" no hover */
}

/* Estilo para o botão desabilitado (se necessário) */
#audioControlButton.audio-control-button:disabled {
    cursor: not-allowed;
    opacity: 0.6;
    background-color: #555;
    box-shadow: none;
}

/* Estilo para o ícone de música dentro do botão */
.audio-control-button .audio-icon {
    font-size: 2.2em; /* Tamanho do ícone */
    position: absolute; /* Use absolute para sobrepô-los no mesmo lugar */
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%); /* Centraliza o ícone perfeitamente */
    z-index: 3; /* Acima do neon e do progresso */
    transition: opacity 0.3s ease, transform 0.3s ease; /* Transição para fade e rotação */
}

/* Definindo a visibilidade inicial dos ícones */
.audio-control-button .icon-on {
    display: none; /* Inicia escondido */
    opacity: 0;
}

.audio-control-button .icon-off {
    display: inline-block; /* Inicia visível */
    opacity: 1;
}

/* Ícone girando e visível quando a música está tocando */
#audioControlButton.is-playing .icon-on {
    display: inline-block; /* Torna visível */
    opacity: 1;
    animation: icon-spin 2s linear infinite; /* Animação de rotação contínua */
}

/* Esconde o ícone de 'mute' quando a música está tocando */
#audioControlButton.is-playing .icon-off {
    display: none;
    opacity: 0;
    animation: none; /* Garante que não gire se estava girando */
}

/* Quando a música está pausada/desligada, o 'icon-on' deve estar invisível */
#audioControlButton:not(.is-playing) .icon-on {
    display: none;
    opacity: 0;
    animation: none;
}

/* Quando a música está pausada/desligada, o 'icon-off' deve estar visível */
#audioControlButton:not(.is-playing) .icon-off {
    display: inline-block;
    opacity: 1;
}

/* Keyframes para a animação de rotação do ícone */
@keyframes icon-spin {
    0% { transform: translate(-50%, -50%) rotate(0deg); }
    100% { transform: translate(-50%, -50%) rotate(360deg); }
}

/* Estilo do botão quando a música está tocando (classe 'is-playing' adicionada via JS) */
#audioControlButton.is-playing {
    box-shadow: 0 0 10px rgba(var(--neon-flame-color1-rgb), 0.8),
                0 0 20px rgba(var(--neon-flame-color2-rgb), 0.6),
                0 0 30px rgba(var(--neon-flame-color1-rgb), 0.4);
    animation: pulse-border 1.5s infinite alternate;
}

/* Keyframes para a animação de pulsação da borda */
@keyframes pulse-border {
    0% { box-shadow: 0 0 10px rgba(var(--neon-flame-color1-rgb), 0.8), 0 0 20px rgba(var(--neon-flame-color2-rgb), 0.6); }
    100% { box-shadow: 0 0 15px rgba(var(--neon-flame-color2-rgb), 1), 0 0 25px rgba(var(--neon-flame-color1-rgb), 0.8); }
}

/* Estilo para o elemento SVG que cria o arco de progresso circular */
.audio-progress-arc {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    transform: rotate(-90deg);
    z-index: 1;
    pointer-events: none;
    opacity: 0;
    transition: opacity 0.3s ease;
}

/* Torna o arco de progresso visível quando a música está tocando */
#audioControlButton.is-playing .audio-progress-arc {
    opacity: 1;
}

/* Estilo do fundo do arco de progresso (a parte não preenchida) */
.audio-progress-arc .arc-background {
    stroke: rgba(255, 255, 255, 0.1);
    stroke-width: 3.5;
    fill: none;
}

/* Estilo do progresso do arco (a parte preenchida) */
.audio-progress-arc .arc-progress {
    stroke: var(--cor-destaque);
    stroke-width: 3.5;
    fill: none;
    stroke-linecap: round;
    transition: stroke-dasharray 0.1s linear;
}

/* Elemento para o efeito de "chama" neon que se move */
.audio-control-button .neon-effect {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(
        45deg,
        var(--neon-flame-color1) 0%,
        var(--neon-flame-color2) 25%,
        var(--neon-flame-color1) 50%,
        var(--neon-flame-color2) 75%,
        var(--neon-flame-color1) 100%
    );
    background-size: 200% 100%;
    filter: blur(8px);
    opacity: 0;
    transition: opacity 0.4s ease;
    border-radius: inherit;
    z-index: 0;
    pointer-events: none;
    animation: neon-gradient-spin 10s linear infinite;
}

/* Ativa o efeito neon quando a música está tocando */
#audioControlButton.is-playing .neon-effect {
    opacity: 0.7;
}

/* Keyframes para a animação de rotação do gradiente neon */
@keyframes neon-gradient-spin {
    0% { transform: rotate(0deg); }
    100% { transform: rotate(360deg); }
}

/* Estilos para o novo elemento de exibição de texto da música */
.music-title-display {
    background-color: rgba(0, 0, 0, 0.6);
    color: white;
    padding: 8px 12px;
    border-radius: 5px;
    font-size: 14px;
    z-index: 999;
    white-space: nowrap;
    pointer-events: none;
    opacity: 1; /* Texto sempre visível ao iniciar */
    transition: opacity 0.3s ease;
    margin-top: 0;
    margin-bottom: 0;
    margin-left: 0;
    margin-right: 0;
}

.audio-control-button-container:hover .music-title-display {
    opacity: 1; /* Garante que permaneça visível no hover */
}

/* ===================================== */
/* 13. Mensagem Central na Tela          */
/* ===================================== */
.central-message {
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    background-color: rgba(0, 0, 0, 0.8); /* Fundo semitransparente escuro */
    color: var(--cor-btn-text);
    padding: 20px 30px;
    border-radius: var(--border-radius);
    font-family: var(--fonte-titulo);
    font-size: 1.8em; /* Tamanho maior para destaque */
    font-weight: bold;
    text-align: center;
    opacity: 0; /* Inicialmente invisível */
    visibility: hidden; /* Esconde completamente quando não está visível */
    transition: opacity 0.5s ease-in-out, visibility 0.5s ease-in-out; /* Transição suave */
    z-index: 1060; /* Acima de tudo */
    pointer-events: none; /* Não deve interferir com cliques */
    white-space: nowrap; /* Impede quebra de linha */
    box-shadow: 0 0 15px rgba(var(--neon-flame-color2-rgb), 0.5); /* Sombra sutil neon */
}

/* Classe para mostrar a mensagem */
.central-message.show {
    opacity: 1;
    visibility: visible;
}

/* ===================================== */
/* 14. Estilos dos Cards (Exemplo)       */
/* ===================================== */
.card-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 20px;
    padding: 20px;
    max-width: 1200px;
    margin: 50px auto; /* Centraliza a grade */
}

.card {
    background-color: var(--cor-fundo-card);
    color: var(--cor-texto-card);
    padding: 25px;
    border-radius: var(--border-radius);
    box-shadow: var(--sombra-leve);
    text-align: center;
    transition: transform 0.3s ease, box-shadow 0.3s ease, border-color 0.3s ease; /* Transições para hover */
    border: 2px solid transparent; /* Borda transparente inicial */
    cursor: pointer; /* Indica que é clicável */
}

.card:hover {
    transform: translateY(-8px); /* Eleva o card */
    box-shadow: 0 12px 25px rgba(0, 0, 0, 0.3); /* Sombra mais pronunciada */
    border-color: var(--cor-borda-card-hover); /* Muda a cor da borda no hover */
}

.card-icon {
    font-size: 3em;
    color: var(--cor-primaria); /* Cor do ícone do card */
    margin-bottom: 15px;
}

.card h3 {
    margin-top: 0;
    color: var(--cor-destaque);
}

.card ul {
    list-style: none;
    padding: 0;
    margin: 15px 0 0;
    font-size: 0.95em;
    line-height: 1.6;
}

.card ul li {
    margin-bottom: 5px;
}

/* ===================================== */
/* 15. Seção de Segurança (NOVA SEÇÃO)   */
/* ===================================== */

.security-features-grid {
    display: grid;
    gap: var(--gap-grid); /* Garante o espaçamento entre os cards */
    margin-top: 50px;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); /* Responsividade para 3 colunas */
}

.security-card {
    background-color: var(--cor-card-bg); /* Fundo azul escuro para o card */
    border-radius: var(--border-radius); /* Bordas arredondadas */
    padding: var(--padding-card); /* Preenchimento interno */
    text-align: center;
    box-shadow: var(--sombra-leve); /* Sombra suave */
    transition: var(--transicao-card-hover); /* Transição para o efeito hover */
    border: 1px solid rgba(255, 255, 255, 0.05); /* Borda sutil */
    display: flex; /* Flexbox para alinhar conteúdo verticalmente */
    flex-direction: column;
    justify-content: space-between; /* Espaçamento entre os itens do card */
}

/* Efeito hover para os cards de segurança */
.security-card:hover {
    transform: translateY(-5px) rotate(0.8deg); /* Eleva e rotaciona um pouco */
    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.6); /* Sombra mais intensa no hover */
    background-color: var(--cor-card-hover); /* Muda a cor de fundo no hover */
}

.security-card .card-icon {
    /* FontAwesome icons don't need width/height like images */
    margin: 0 auto 20px auto; /* Centraliza e adiciona espaço abaixo */
    display: block;
    transition: none; /* Garante que o ícone não anime individualmente no hover do card */
    color: var(--cor-primaria); /* Cor verde vibrante para o ícone */
    font-size: 3.5em; /* Tamanho grande para o ícone */
    line-height: 1; /* Ajuste para alinhamento vertical */
}

/* ===================================== */
/* 16. Media Queries (Responsividade)    */
/* ===================================== */

@media (max-width: 992px) {
    /* Ajustes Gerais */
    h1 { font-size: 3em; }
    h2 { font-size: 2.2em; }
    h3 { font-size: 1.8em; }

    .section-padded {
        padding: 80px 0; /* Reduz padding em telas menores */
    }

    .container {
        padding: 0 15px; /* Ajusta padding lateral */
    }

    /* Header e Navegação */
    .header-content {
        flex-wrap: nowrap; /* Impede quebra de linha do header em 992px */
    }

    .main-nav {
        /* Esconde a navegação por padrão */
        display: none;
        flex-direction: column;
        width: 100%; /* Ocupa a largura total */
        position: absolute;
        top: 80px; /* Abaixo do header */
        left: 0;
        background-color: var(--cor-fundo-escuro);
        box-shadow: var(--sombra-leve);
        padding: 20px;
        border-top: 1px solid var(--cor-bordas);
        z-index: 999; /* Acima do conteúdo, abaixo do menu-toggle */
    }

    .main-nav.active {
        display: flex; /* Mostra a navegação quando a classe 'active' está presente */
    }

    .main-nav ul {
        flex-direction: column;
        gap: 15px;
        width: 100%;
        align-items: center; /* Centraliza itens no menu mobile */
    }

    .main-nav ul li a {
        padding: 10px 0;
        width: fit-content; /* Ajusta a largura ao conteúdo */
        text-align: center;
    }

    .menu-toggle {
        display: block; /* Mostra o botão de hambúrguer */
    }

    /* Seção Hero */
    .hero-section {
        height: 550px; /* Altura menor para o hero em tablets */
    }

    .hero-content h1 {
        font-size: 3em;
    }

    .server-branding {
        flex-direction: column;
        gap: 15px;
    }

    .server-banner {
        max-width: 200px;
    }

    .server-icon {
        width: 80px;
        height: 80px;
    }

    .hero-subtitle {
        font-size: 1.1em;
    }

    /* Layout de Duas Colunas */
    .content-two-columns {
        flex-direction: column; /* Empilha as colunas verticalmente */
        align-items: center;
        text-align: center;
    }

    .content-two-columns .text-content,
    .content-two-columns .image-content {
        min-width: unset; /* Remove o min-width para flexibilidade total */
        width: 100%; /* Ocupa a largura total */
    }

    .responsive-image {
        max-width: 80%; /* Reduz o tamanho da imagem responsiva um pouco */
        margin: 20px auto; /* Centraliza a imagem */
    }

    .section-description {
        font-size: 1em;
        margin-bottom: 30px;
    }

    /* Cards e Grids (ajustes para garantir que se ajustem) */
    .max-2-columns,
    .max-3-columns,
    .max-4-columns,
    .services-grid,
    .roles-grid,
    .access-cards-grid,
    .community-grid,
    .events-sub-grid,
    .partnership-grid,
    .security-features-grid,
    .card-grid {
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); /* Permite 1 ou 2 colunas */
    }

    /* Controle de Áudio */
    .audio-control-button-container {
        flex-direction: column-reverse; /* Inverte a ordem para o texto ficar acima do botão */
        align-items: flex-end; /* Alinha o texto à direita do container */
        bottom: 15px;
        right: 15px;
        gap: 8px; /* Reduz o espaço entre o texto e o botão */
    }

    .music-title-display {
        position: static; /* Remove o posicionamento fixo para o flexbox controlar */
        transform: none; /* Remove qualquer transformação */
        right: auto;
        bottom: auto;
        opacity: 1; /* Mantém sempre visível em telas menores para não depender de hover */
        pointer-events: auto; /* Permite cliques em caso de necessidade de copiar */
        text-align: right; /* Alinha o texto à direita */
        padding: 5px 10px; /* Ajusta o padding */
        font-size: 13px; /* Reduz o tamanho da fonte */
    }

    /* Remove o efeito de hover para o texto em telas menores, se ele estiver sempre visível */
    .audio-control-button-container:hover .music-title-display {
        opacity: 1;
    }

    /* Mensagem Central */
    .central-message {
        font-size: 1.5em; /* Reduz o tamanho da fonte */
        padding: 15px 25px; /* Reduz o padding */
    }
}

@media (max-width: 768px) {
    h1 { font-size: 2.5em; }
    h2 { font-size: 2em; }
    h3 { font-size: 1.6em; }
    h4 { font-size: 1.4em; }

    .hero-section {
        height: 450px; /* Altura ainda menor para mobile */
    }

    .hero-content h1 {
        font-size: 2.5em;
    }

    .hero-subtitle {
        font-size: 1em;
        margin-bottom: 20px;
    }

    .btn-primary {
        padding: 12px 25px;
        font-size: 1em;
    }

    /* Grids sempre em uma coluna */
    .max-2-columns,
    .max-3-columns,
    .max-4-columns,
    .services-grid,
    .roles-grid,
    .access-cards-grid,
    .community-grid,
    .events-sub-grid,
    .partnership-grid,
    .security-features-grid,
    .card-grid {
        grid-template-columns: 1fr; /* Força uma única coluna */
    }

    .section-padded {
        padding: 40px 0; /* Mais redução no padding */
    }

    /* Cards */
    .service-card,
    .role-category-card,
    .access-card,
    .community-card,
    .partnership-card,
    .event-card,
    .security-card,
    .card {
        padding: 20px; /* Reduz padding interno dos cards */
    }

    .card-title {
        font-size: 1.3em;
    }

    .card-description {
        font-size: 0.9em;
    }

    /* Controle de Áudio */
    .audio-control-button-container {
        bottom: 10px;
        right: 10px;
        gap: 5px;
    }

    #audioControlButton.audio-control-button {
        width: 50px;
        height: 50px;
    }

    .audio-control-button .audio-icon {
        font-size: 1.8em;
    }

    .music-title-display {
        font-size: 12px;
        padding: 5px 8px;
    }

    /* Rodapé */
    .main-footer {
        padding: 20px 0;
        font-size: 0.8em;
    }
}

@media (max-width: 480px) {
    h1 { font-size: 2em; }
    h2 { font-size: 1.8em; }
    h3 { font-size: 1.4em; }

    .hero-section {
        height: 380px; /* Altura mínima para mobile menor */
    }

    .hero-content h1 {
        font-size: 2em;
    }

    .server-banner {
        max-width: 150px;
    }

    .server-icon {
        width: 70px;
        height: 70px;
    }

    .btn-primary {
        padding: 10px 20px;
        font-size: 0.9em;
    }

    .central-message {
        font-size: 1.2em;
        padding: 10px 20px;
    }

}
/* Estilos gerais para o corpo da página (apenas para contexto) */
body {
    font-family: Arial, sans-serif;
    background-color: #1a1a1a; /* Fundo escuro */
    color: #f0f0f0;
    margin: 0;
    padding: 0;
    overflow-x: hidden; /* Evita barra de rolagem horizontal */
}

/* Reset Básico para consistência entre navegadores */
*, *::before, *::after {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: var(--fonte-corpo);
    line-height: 1.6;
    color: var(--cor-texto-claro);
    background-color: var(--cor-fundo-escuro);
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
}

/* Base para links */
a {
    color: var(--cor-link);
    text-decoration: none;
    transition: var(--transicao-padrao);
}

a:hover {
    color: var(--cor-link-hover);
    text-decoration: underline;
}

/* Base para títulos */
h1, h2, h3, h4, h5, h6 {
    font-family: var(--fonte-titulo);
    color: var(--cor-texto-claro);
    margin-bottom: 0.8em;
    line-height: 1.2;
}

h1 { font-size: 3.5em; }
h2 { font-size: 2.8em; }
h3 { font-size: 2.2em; }
h4 { font-size: 1.8em; }

p {
    margin-bottom: 1em;
}

ul {
    list-style: none;
}

/* Classes de utilidade para espaçamento */
.margin-bottom-30 { margin-bottom: 30px !important; }
.margin-bottom-40 { margin-bottom: 40px !important; }
.text-center { text-align: center; }

/* ===================================== */
/* 1. Layout Geral e Contêineres         */
/* ===================================== */

.container {
    max-width: var(--max-width-content);
    margin: 0 auto;
    padding: 0 20px;
}

.section-padded {
    padding: var(--padding-section); /* Onde --padding-section é 80px 0; */
}

.section-alt-bg {
    background-color: var(--cor-terciaria); /* Fundo verde mais escuro */
}

/* Flexbox para duas colunas */
.content-two-columns {
    display: flex;
    flex-wrap: wrap; /* Permite quebrar linha em telas menores */
    gap: var(--gap-grid);
    align-items: center;
}

.content-two-columns .text-content,
.content-two-columns .image-content {
    flex: 1; /* Permite que ambos os itens cresçam igualmente */
    min-width: 300px; /* Garante que os itens não fiquem muito estreitos */
}

.responsive-image {
    max-width: 100%;
    height: auto;
    display: block; /* Remove espaçamento extra abaixo da imagem */
    border-radius: var(--border-radius);
}

.bordered-image {
    border: 3px solid var(--cor-primaria); /* Borda primária para imagens de destaque */
    box-shadow: var(--sombra-card);
}

/* ===================================== */
/* 2. Cabeçalho (Header)                 */
/* ===================================== */

.main-header {
    background-color: var(--cor-fundo-escuro);
    padding: 20px 0;
    box-shadow: var(--sombra-leve);
    position: sticky;
    top: 0;
    z-index: 1000;
    border-bottom: 1px solid var(--cor-bordas);
}

.header-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap; /* Para navegação em telas menores */
}

.header-logo {
    font-family: var(--fonte-titulo);
    font-size: 1.8em;
    color: var(--cor-primaria);
    text-transform: uppercase;
    font-weight: 700;
    letter-spacing: 1px;
}

.main-nav ul {
    display: flex;
    gap: 25px;
}

.main-nav ul li a {
    color: var(--cor-texto-claro);
    font-weight: 700;
    padding: 5px 0;
    position: relative;
}

.main-nav ul li a::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 2px;
    background-color: var(--cor-destaque);
    transition: width 0.3s ease;
}

.main-nav ul li a:hover::after {
    width: 100%;
}

/* NOVO: Botão de Hambúrguer para Mobile */
.menu-toggle {
    display: none; /* Escondido por padrão em desktop */
    font-size: 2em;
    color: var(--cor-texto-claro);
    background: none;
    border: none;
    cursor: pointer;
    padding: 10px;
    z-index: 1001; /* Acima do nav quando aberto */
}

/* ===================================== */
/* 3. Seção Hero (Destaque Principal)    */
/* ===================================== */

.hero-section {
    position: relative;
    height: 650px; /* Altura fixa para o hero */
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    color: var(--cor-texto-claro);
    overflow: hidden; /* Garante que a imagem de fundo não vaze */
    background: url('https://matheuzera.github.io/ZerasCraft/images/site/banner-fundo.gif') no-repeat center center/cover; /* Imagem de fundo do hero */
}

.hero-image-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.7); /* Escurece a imagem de fundo */
    z-index: 1;
}

.hero-content {
    position: relative;
    z-index: 2;
    padding: 20px;
    max-width: 900px;
}

.server-branding {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 20px;
    margin-bottom: 20px;
}

.server-banner {
    max-width: 250px;
    border-radius: var(--border-radius);
    box-shadow: var(--sombra-leve);
}

.server-icon {
    width: 100px;
    height: 100px;
    border-radius: 50%; /* Torna o ícone circular */
    border: 4px solid var(--cor-primaria);
    box-shadow: var(--sombra-card);
}

.hero-content h1 {
    font-size: 3.8em;
    margin-bottom: 0.5em;
    text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.5);
}

.hero-subtitle {
    font-size: 1.3em;
    max-width: 700px;
    margin: 0 auto 30px auto;
    color: rgba(255, 255, 255, 0.9);
}

/* ===================================== */
/* 4. Botões e Elementos Interativos     */
/* ===================================== */

.btn-primary {
    display: inline-block;
    background-color: var(--cor-primaria);
    color: var(--cor-btn-text);
    padding: 15px 30px;
    border-radius: var(--border-radius);
    text-transform: uppercase;
    font-weight: 700;
    letter-spacing: 1px;
    transition: var(--transicao-padrao);
    border: none;
    cursor: pointer;
    font-size: 1.1em;
}

.btn-primary:hover {
    background-color: var(--cor-secundaria);
    transform: translateY(-3px);
    box-shadow: var(--sombra-leve);
    text-decoration: none;
}

.btn-link {
    display: inline-block;
    color: var(--cor-primaria);
    font-weight: 700;
    text-decoration: underline;
    margin-top: 15px;
    transition: var(--transicao-padrao);
}

.btn-link:hover {
    color: var(--cor-destaque);
    text-decoration: none;
    transform: translateX(5px);
}

.discord-channel-link {
    color: var(--cor-link);
    font-weight: bold;
}

.discord-channel-link:hover {
    color: var(--cor-link-hover);
    text-decoration: underline;
}

/* Estilo para ícones em títulos */
h2 .fas, h3 .fas {
    color: var(--cor-destaque);
    margin-right: 10px;
}

/* ===================================== */
/* 5. Seções com Grids                   */
/* ===================================== */

.section-description {
    text-align: center;
    font-size: 1.1em;
    max-width: 800px;
    margin: 0 auto 40px auto;
    color: rgba(255, 255, 255, 0.8);
}

/* Estilo base para todos os grids */
.services-grid,
.roles-grid,
.access-cards-grid,
.community-grid,
.events-sub-grid,
.partnership-grid { /* Adicionado partnership-grid */
    display: grid;
    gap: var(--gap-grid);
    margin-top: 50px;
}

.max-2-columns {
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
}

.max-3-columns {
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
}

.max-4-columns {
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
}

/* Estilo base para todos os cards - AGORA INCLUI TRANSFORMAÇÕES DE HOVER AQUI */
.service-card,
.role-category-card,
.access-card,
.community-card,
.partnership-card,
.event-card,
.security-card { /* Adicionado security-card */
    background-color: var(--cor-card-bg);
    border-radius: var(--border-radius);
    padding: var(--padding-card);
    text-align: center;
    box-shadow: var(--sombra-leve);
    transition: var(--transicao-card-hover); /* Usando a variável de transição para cards */
    border: 1px solid rgba(255, 255, 255, 0.05); /* Borda sutil */
    display: flex; /* Para flexibilizar conteúdo interno */
    flex-direction: column;
    justify-content: space-between; /* Empurra o botão para baixo */
}

/* Efeito hover para cards - APLICADO AO PRÓPRIO CARD */
.service-card:hover,
.role-category-card:hover,
.access-card:hover,
.community-card:hover,
.partnership-card:hover,
.event-card:hover,
.security-card:hover { /* Adicionado security-card:hover */
    transform: translateY(-5px) rotate(0.8deg); /* Card sobe um pouco e rotaciona 2 graus */
    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.6); /* Sombra mais intensa e um pouco maior */
    background-color: var(--cor-card-hover); /* Fundo do card muda */
}

/* Estilos para os ícones e imagens dentro dos cards (estáticos no hover do card) */
.service-card .card-icon,
.role-category-card .role-category-icon,
.community-card .member-avatar,
.partnership-card .partnership-logo,
.access-card .card-icon,
.security-card .card-icon { /* Adicionado security-card .card-icon */
    width: 80px; /* Tamanho padrão para ícones de card */
    height: 80px;
    object-fit: cover;
    border-radius: var(--border-radius);
    margin: 0 auto 20px auto; /* Centraliza e adiciona espaço abaixo */
    display: block; /* Garante que a transformação funcione corretamente */
    transition: none; /* REMOVE A TRANSIÇÃO NO HOVER DO ÍCONE, ELE FICARÁ ESTÁTICO */
}

/* Exceção para avatares e logos que são circulares */
.member-avatar,
.partnership-logo {
    border-radius: 50%; /* Avatares circulares */
    width: 100px;
    height: 100px;
}

/* Tratamento específico para as imagens de evento (estáticas no hover do card) */
.event-card .event-card-image {
    width: 100%;
    height: 200px;
    object-fit: cover;
    border-top-left-radius: var(--border-radius);
    border-top-right-radius: var(--border-radius);
    margin-bottom: 0;
    border-bottom: 2px solid var(--cor-primaria);
    display: block; /* Garante que ocupe o espaço */
    transition: none; /* REMOVE A TRANSIÇÃO NO HOVER DA IMAGEM, ELA FICARÁ ESTÁTICA */
}

.card-title {
    color: var(--cor-destaque);
    font-size: 1.5em;
    margin-bottom: 10px;
}

.card-description {
    color: rgba(255, 255, 255, 0.7);
    font-size: 0.95em;
    flex-grow: 1; /* Permite que o texto ocupe o espaço disponível */
    margin-bottom: 20px;
}

/* ===================================== */
/* 6. Seção de Cargos e Canais           */
/* ===================================== */

.role-category-card .role-list {
    text-align: left;
    margin-top: 15px;
    padding-left: 20px;
    list-style: disc; /* Adiciona marcadores de lista */
    color: rgba(255, 255, 255, 0.7);
    font-size: 0.9em;
}

.role-category-card .role-list li {
    margin-bottom: 5px;
}

/* ===================================== */
/* 7. Seção Pixel Legends                */
/* ===================================== */

.pixel-legends-embed {
    background-color: var(--cor-card-bg);
    border-radius: var(--border-radius);
    padding: var(--padding-card);
    margin-top: var(--gap-grid);
    box-shadow: var(--sombra-leve);
    border: 1px solid rgba(255, 255, 255, 0.05);
}

/* No seu CSS */

#pixel-legends {
    /* Mantém o padding interno da seção conforme definido em .section-padded ou em ajuste anterior */
    /* Exemplo: padding: 40px 0; */
    margin-bottom: 10px; /* Altere este valor para controlar a distância entre Pixel Legends e a próxima seção */
}

.pixel-legends-embed h4 {
    color: var(--cor-destaque);
    font-size: 1.6em;
    margin-bottom: 1em;
}

.pixel-legends-embed ul {
    list-style: inside;
    margin-bottom: 1em;
    color: rgba(255, 255, 255, 0.8);
}

.pixel-legends-embed li {
    margin-bottom: 8px;
}

/* ===================================== */
/* 8. Seção de Acesso                    */
/* ===================================== */

.access-card code {
    display: block;
    background-color: rgba(0, 0, 0, 0.3);
    padding: 10px;
    border-radius: var(--border-radius);
    margin: 10px auto;
    font-family: 'Consolas', 'Monaco', monospace;
    color: var(--cor-destaque);
    font-size: 1.1em;
    word-break: break-all; /* Garante que o texto quebre se for muito longo */
}

/* ===================================== */
/* 9. Seção de Eventos                   */
/* ===================================== */

.events-sub-grid {
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
}

.event-card {
    overflow: hidden; /* Esconde partes da imagem que podem vazar */
}

.event-card-inner {
    padding: var(--padding-card);
    text-align: left; /* Alinhamento de texto para o conteúdo do card */
    flex-grow: 1;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
}

.event-card-inner h3 {
    text-align: center;
    font-size: 1.6em;
    margin-bottom: 15px;
    min-height: 2.5em; /* Garante altura mínima para títulos de duas linhas */
}

.event-card-inner p {
    font-size: 0.9em;
    color: rgba(255, 255, 255, 0.7);
    margin-bottom: 10px;
}

.event-details {
    font-style: italic;
    font-size: 0.85em;
    color: rgba(255, 255, 255, 0.6);
    margin-top: 15px;
    margin-bottom: 20px;
}

/* ===================================== */
/* 10. Seção de Comunidade e Parcerias   */
/* ===================================== */

.partnership-proposal-card {
    background-color: var(--cor-terciaria);
    border-radius: var(--border-radius);
    padding: var(--padding-card);
    text-align: center;
    margin-top: var(--gap-grid);
    box-shadow: var(--sombra-card);
    border: 1px solid var(--cor-primaria);
}

.partnership-proposal-card h3 {
    color: var(--cor-destaque);
    margin-bottom: 15px;
}

.partnership-proposal-card p {
    color: rgba(255, 255, 255, 0.8);
    font-size: 1.05em;
    margin-bottom: 30px;
}

/* ===================================== */
/* 11. Rodapé (Footer)                   */
/* ===================================== */

.main-footer {
    background-color: var(--cor-fundo-escuro);
    padding: 30px 0;
    text-align: center;
    border-top: 1px solid var(--cor-bordas);
    color: rgba(255, 255, 255, 0.6);
    font-size: 0.9em;
}

.footer-logo {
    font-family: var(--fonte-titulo);
    font-size: 1.5em;
    color: var(--cor-primaria);
    font-weight: 700;
    display: block;
    margin-bottom: 10px;
}

/* ===================================== */
/* 12. Controle de Áudio - NOVO SISTEMA  */
/* ===================================== */

/* Estilos para o contêiner do botão e texto */
.audio-control-button-container {
    position: fixed; /* ou absolute, dependendo de onde você quer que ele fique */
    bottom: 20px;
    right: 20px;
    display: flex; /* Para alinhar o botão e o texto */
    flex-direction: column; /* Para empilhar texto e botão */
    align-items: center; /* Centraliza horizontalmente */
    z-index: 1000; /* Garante que fique acima de outros elementos */
}

/* Estilos para o display do título da música */
.music-title-display {
    background-color: rgba(0, 0, 0, 0.7);
    color: #fff;
    padding: 5px 10px;
    border-radius: 5px;
    margin-bottom: 5px; /* Espaço entre o texto e o botão */
    font-size: 0.9em;
    white-space: nowrap; /* Evita que o texto quebre linha */
    opacity: 1; /* Deve estar visível por padrão, ou animar com o JS */
    transition: opacity 0.3s ease; /* Transição para sumir/aparecer */
}

/* Estilos para o botão de controle de áudio */
.audio-control-button {
    position: relative; /* Importante para posicionar os ícones e SVG */
    width: 60px; /* Ajuste o tamanho conforme necessário */
    height: 60px;
    border-radius: 50%;
    background-color: var(--primary-color); /* Use sua cor primária */
    border: none;
    cursor: pointer;
    display: flex;
    justify-content: center;
    align-items: center;
    overflow: hidden; /* Garante que o neon não vaze */
    box-shadow: 0 0 10px rgba(0, 255, 0, 0.5); /* Sombra para o efeito base */
    transition: all 0.3s ease; /* Transição para o botão */
}

/* Efeito Neon */
.audio-control-button .neon-effect {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    border-radius: 50%;
    background: radial-gradient(circle at center, rgba(0, 255, 0, 0.6) 0%, rgba(0, 255, 0, 0.3) 50%, transparent 70%);
    opacity: 0; /* Começa invisível */
    transition: opacity 0.5s ease-in-out;
    filter: blur(5px); /* Suaviza o brilho */
}

/* Estado 'is-playing' para o botão (quando a música está tocando) */
.audio-control-button.is-playing .neon-effect {
    opacity: 1; /* Ativa o neon quando a música toca */
    animation: pulse-neon 1.5s infinite alternate; /* Adiciona uma animação de pulso */
}

@keyframes pulse-neon {
    from {
        box-shadow: 0 0 10px var(--primary-color-light);
    }
    to {
        box-shadow: 0 0 20px var(--primary-color-light), 0 0 30px var(--primary-color-light);
    }
}


/* Ícones */
.audio-icon {
    position: absolute; /* Posiciona os ícones um sobre o outro */
    font-size: 28px; /* Ajuste o tamanho dos ícones */
    color: #fff; /* Cor dos ícones */
    transition: opacity 0.3s ease, transform 0.3s ease; /* Transição suave para os ícones */
}

/* O ícone ON (volume-up) é visível quando a música toca */
.audio-control-button .icon-on {
    display: none; /* Começa escondido, o JS controla */
}

/* O ícone OFF (volume-mute) é visível quando a música está pausada */
.audio-control-button .icon-off {
    display: inline-block; /* Começa visível, o JS controla */
}

/* Quando o botão está no estado 'is-playing' */
.audio-control-button.is-playing .icon-on {
    display: inline-block; /* Mostra o ícone ON */
}

.audio-control-button.is-playing .icon-off {
    display: none; /* Esconde o ícone OFF */
}

/* SVG do Progresso */
.audio-progress-arc {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    transform: rotate(-90deg); /* Começa o arco na parte superior */
}

.arc-background {
    fill: none;
    stroke: rgba(255, 255, 255, 0.2);
    stroke-width: 4;
}

.arc-progress {
    fill: none;
    stroke: yellow; /* Cor do progresso */
    stroke-width: 4;
    stroke-linecap: round; /* Pontas arredondadas para o progresso */
    transition: stroke-dashoffset 0.5s linear; /* Animação suave para o progresso */
    /* stroke-dasharray será definido pelo JS */
}

/* Estilos para a mensagem central */
.central-message {
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    background-color: rgba(0, 0, 0, 0.8);
    color: white;
    padding: 15px 25px;
    border-radius: 8px;
    font-size: 1.2em;
    z-index: 1001;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.5s ease, visibility 0.5s ease;
    white-space: nowrap; /* Impede que o texto quebre linha */
}

.central-message.show {
    opacity: 1;
    visibility: visible;
}

/* Variáveis de cor (se você as estiver usando) */
:root {
    --primary-color: #28a745; /* Um verde exemplo */
    --primary-color-light: #4caf50;
    --dark-bg: #1c1c1c;
    --light-text: #f0f0f0;
    --card-bg: #2a2a3a;
}

/* ===================================== */
/* 13. Mensagem Central na Tela          */
/* ===================================== */
.central-message {
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    background-color: rgba(0, 0, 0, 0.8); /* Fundo semitransparente escuro */
    color: var(--cor-btn-text);
    padding: 20px 30px;
    border-radius: var(--border-radius);
    font-family: var(--fonte-titulo);
    font-size: 1.8em; /* Tamanho maior para destaque */
    font-weight: bold;
    text-align: center;
    opacity: 0; /* Inicialmente invisível */
    visibility: hidden; /* Esconde completamente quando não está visível */
    transition: opacity 0.5s ease-in-out, visibility 0.5s ease-in-out; /* Transição suave */
    z-index: 1060; /* Acima de tudo */
    pointer-events: none; /* Não deve interferir com cliques */
    white-space: nowrap; /* Impede quebra de linha */
    box-shadow: 0 0 15px rgba(var(--neon-flame-color2-rgb), 0.5); /* Sombra sutil neon */
}

/* Classe para mostrar a mensagem */
.central-message.show {
    opacity: 1;
    visibility: visible;
}

/* ===================================== */
/* 14. Estilos dos Cards (Exemplo)       */
/* ===================================== */
.card-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 20px;
    padding: 20px;
    max-width: 1200px;
    margin: 50px auto; /* Centraliza a grade */
}

.card {
    background-color: var(--cor-fundo-card);
    color: var(--cor-texto-card);
    padding: 25px;
    border-radius: var(--border-radius);
    box-shadow: var(--sombra-leve);
    text-align: center;
    transition: transform 0.3s ease, box-shadow 0.3s ease, border-color 0.3s ease; /* Transições para hover */
    border: 2px solid transparent; /* Borda transparente inicial */
    cursor: pointer; /* Indica que é clicável */
}

.card:hover {
    transform: translateY(-8px); /* Eleva o card */
    box-shadow: 0 12px 25px rgba(0, 0, 0, 0.3); /* Sombra mais pronunciada */
    border-color: var(--cor-borda-card-hover); /* Muda a cor da borda no hover */
}

.card-icon {
    font-size: 3em;
    color: var(--cor-primaria); /* Cor do ícone do card */
    margin-bottom: 15px;
}

.card h3 {
    margin-top: 0;
    color: var(--cor-destaque);
}

.card ul {
    list-style: none;
    padding: 0;
    margin: 15px 0 0;
    font-size: 0.95em;
    line-height: 1.6;
}

.card ul li {
    margin-bottom: 5px;
}

/* ===================================== */
/* 15. Seção de Segurança (NOVA SEÇÃO)   */
/* ===================================== */

.security-features-grid {
    display: grid;
    gap: var(--gap-grid); /* Garante o espaçamento entre os cards */
    margin-top: 50px;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); /* Responsividade para 3 colunas */
}

.security-card {
    background-color: var(--cor-card-bg); /* Fundo azul escuro para o card */
    border-radius: var(--border-radius); /* Bordas arredondadas */
    padding: var(--padding-card); /* Preenchimento interno */
    text-align: center;
    box-shadow: var(--sombra-leve); /* Sombra suave */
    transition: var(--transicao-card-hover); /* Transição para o efeito hover */
    border: 1px solid rgba(255, 255, 255, 0.05); /* Borda sutil */
    display: flex; /* Flexbox para alinhar conteúdo verticalmente */
    flex-direction: column;
    justify-content: space-between; /* Espaçamento entre os itens do card */
}

/* Efeito hover para os cards de segurança */
.security-card:hover {
    transform: translateY(-5px) rotate(0.8deg); /* Eleva e rotaciona um pouco */
    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.6); /* Sombra mais intensa no hover */
    background-color: var(--cor-card-hover); /* Muda a cor de fundo no hover */
}

.security-card .card-icon {
    /* FontAwesome icons don't need width/height like images */
    margin: 0 auto 20px auto; /* Centraliza e adiciona espaço abaixo */
    display: block;
    transition: none; /* Garante que o ícone não anime individualmente no hover do card */
    color: var(--cor-primaria); /* Cor verde vibrante para o ícone */
    font-size: 3.5em; /* Tamanho grande para o ícone */
    line-height: 1; /* Ajuste para alinhamento vertical */
}

/* ===================================== */
/* 16. Media Queries (Responsividade)    */
/* ===================================== */

@media (max-width: 992px) {
    /* Ajustes Gerais */
    h1 { font-size: 3em; }
    h2 { font-size: 2.2em; }
    h3 { font-size: 1.8em; }

    .section-padded {
        padding: var(--padding-section); /* Voltar ao valor padrão do :root */
        /* Removi o 'padding: 60px 0;' aqui para usar o valor global (80px 0 ou o que você definir) */
    }

    .container {
        padding: 0 15px; /* Ajusta padding lateral */
    }

    /* Header e Navegação */
    .header-content {
        flex-wrap: nowrap;
    }

    .main-nav {
        display: none;
        flex-direction: column;
        width: 100%;
        position: absolute;
        top: 80px;
        left: 0;
        background-color: var(--cor-fundo-escuro);
        box-shadow: var(--sombra-leve);
        padding: 20px;
        border-top: 1px solid var(--cor-bordas);
        z-index: 999;
    }

    .main-nav.active {
        display: flex;
    }

    .main-nav ul {
        flex-direction: column;
        gap: 15px;
        width: 100%;
        align-items: center;
    }

    .main-nav ul li a {
        padding: 10px 0;
        width: fit-content;
        text-align: center;
    }

    .menu-toggle {
        display: block;
    }

    /* Seção Hero */
    .hero-section {
        height: 550px;
    }

    .hero-content h1 {
        font-size: 3em;
    }

    .server-branding {
        flex-direction: column;
        gap: 15px;
    }

    .server-banner {
        max-width: 200px;
    }

    .server-icon {
        width: 80px;
        height: 80px;
    }

    .hero-subtitle {
        font-size: 1.1em;
    }

    /* Layout de Duas Colunas - Manter como desktop ou ajustar minimamente */
    .content-two-columns {
        /* Para manter mais parecido com desktop, podemos tentar manter a linha */
        flex-direction: row; /* Tenta manter em linha, mas pode forçar scroll horizontal */
        flex-wrap: wrap; /* Permite quebrar se não houver espaço suficiente */
        justify-content: center; /* Centraliza se houver quebra */
        gap: var(--gap-grid); /* Mantém o gap entre elas */
    }

    .content-two-columns .text-content,
    .content-two-columns .image-content {
        /* Permite que eles diminuam, mas sem se empilhar totalmente se possível */
        flex: 1 1 45%; /* Tenta manter 2 colunas, cada uma com 45% de largura */
        min-width: 280px; /* Garante um tamanho mínimo para não ficar muito pequeno */
        text-align: left; /* Restaura alinhamento padrão */
    }

    .responsive-image {
        max-width: 100%; /* Permite que a imagem ocupe o espaço disponível */
        margin: 0; /* Remove margem extra */
    }

    .section-description {
        font-size: 1em;
        margin-bottom: 30px;
    }

    /* Cards e Grids: O principal aqui é manter mais de uma coluna */
    .max-2-columns,
    .max-3-columns,
    .max-4-columns,
    .services-grid,
    .roles-grid,
    .access-cards-grid,
    .community-grid,
    .events-sub-grid,
    .partnership-grid,
    .security-features-grid,
    .card-grid {
        /* Tenta manter 2 colunas mesmo em telas menores. */
        /* Se a tela for realmente muito pequena (ex: menos de 600px),
           elas ainda podem quebrar para 1 coluna devido ao minmax. */
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); /* Mantém pelo menos 280px por card */
        /* Se você quer FORÇAR 2 colunas mesmo que os cards fiquem pequenos, use: */
        /* grid-template-columns: repeat(2, 1fr); */
        /* Mas isso pode causar overflow horizontal em telas super estreitas. */
    }

    /* Ajustes específicos para o conteúdo do card para evitar quebra excessiva */
    .card-title {
        font-size: 1.4em; /* Levemente menor que desktop, mas maior que antes em mobile */
        white-space: normal; /* Permite quebrar linha normalmente */
    }

    .card-description {
        font-size: 0.9em; /* Mantém um tamanho legível */
    }

    /* Seção de Eventos (títulos de cards) */
    .event-card-inner h3 {
        font-size: 1.4em; /* Consistente com card-title */
        min-height: unset; /* Remove altura mínima, deixa o conteúdo definir */
    }


    /* Controle de Áudio */
    .audio-control-button-container {
        /* Para telas muito pequenas, talvez seja melhor que o texto não apareça */
        flex-direction: row; /* Tenta manter lado a lado */
        align-items: center;
        bottom: 15px;
        right: 15px;
        gap: 8px;
    }

    .music-title-display {
        /* Para telas menores, podemos esconder o título da música ou fazê-lo menor */
        /* opacity: 0; /* Esconde o texto para telas menores */
        /* font-size: 12px; /* Ou apenas diminua a fonte */
        position: static;
        transform: none;
        right: auto;
        bottom: auto;
        /* Se você realmente quer que ele seja como desktop, deixe opacity: 1; */
        opacity: 1;
        white-space: normal; /* Permite quebra de linha se o título for muito longo */
        padding: 5px 8px; /* Ajusta o padding para telas pequenas */
    }

    /* Adicione mais media queries para tamanhos ainda menores, se necessário */
    @media (max-width: 600px) {
        h1 { font-size: 2.5em; }
        h2 { font-size: 1.8em; }
        h3 { font-size: 1.5em; }

        .hero-section {
            height: 450px; /* Reduz ainda mais o hero */
        }

        .hero-content h1 {
            font-size: 2.5em;
        }

        .hero-subtitle {
            font-size: 1em;
        }

        /* Força os grids a ficarem em uma única coluna para telas muito pequenas */
        .max-2-columns,
        .max-3-columns,
        .max-4-columns,
        .services-grid,
        .roles-grid,
        .access-cards-grid,
        .community-grid,
        .events-sub-grid,
        .partnership-grid,
        .security-features-grid,
        .card-grid {
            grid-template-columns: 1fr; /* Força uma única coluna */
            gap: 20px; /* Reduz o gap entre os cards quando estão empilhados */
        }

        .content-two-columns {
            flex-direction: column; /* Empilha as colunas */
            text-align: center;
        }

        .content-two-columns .text-content,
        .content-two-columns .image-content {
            width: 100%;
        }

        .responsive-image {
            max-width: 90%; /* Ajuste para não ficar tão largo na tela pequena */
            margin: 20px auto;
        }

        /* Reduz o padding dos cards para ficarem mais compactos */
        .service-card,
        .role-category-card,
        .access-card,
        .community-card,
        .partnership-card,
        .event-card,
        .security-card,
        .card {
            padding: 15px; /* Reduz o padding interno dos cards */
        }

        .card-icon {
            font-size: 2.5em; /* Reduz tamanho do ícone */
        }

        .card-title {
            font-size: 1.2em; /* Reduz tamanho do título do card */
        }

        .card-description {
            font-size: 0.85em; /* Reduz tamanho do texto do card */
        }
        
        .audio-control-button-container {
             bottom: 10px;
             right: 10px;
             gap: 5px;
        }

        .music-title-display {
            font-size: 12px;
            padding: 5px 8px;
            white-space: normal; /* Permite quebra de linha */
        }
    }

    @media (max-width: 480px) {
        h1 { font-size: 2em; }
        h2 { font-size: 1.6em; }
        h3 { font-size: 1.3em; }

        .hero-section {
            height: 400px;
        }

        .hero-content h1 {
            font-size: 2em;
        }

        .hero-subtitle {
            font-size: 0.9em;
        }

        .btn-primary {
            padding: 12px 25px;
            font-size: 1em;
        }

        .audio-control-button-container {
            bottom: 5px;
            right: 5px;
            gap: 5px;
        }

        #audioControlButton.audio-control-button {
            width: 50px;
            height: 50px;
        }

        .audio-control-button .audio-icon {
            font-size: 1.8em;
        }

        .music-title-display {
            font-size: 11px;
            padding: 4px 7px;
        }
    }
}